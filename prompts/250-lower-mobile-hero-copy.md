# 250 Lower mobile hero copy

- Date: 2026-05-27
- Purpose: Move the hero copy area lower on mobile viewports.
- Change: Updated the mobile-only `.heroCopy` top offset from `clamp(7.8rem, 14vh, 12.4rem)` to `clamp(10.4rem, 18vh, 15.2rem)`.
- Notes: Desktop and tablet hero positioning were left unchanged.
- Verification: `npm run lint`.
