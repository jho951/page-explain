# 251 Lower mobile hero copy

- Date: 2026-05-27
- Purpose: 모바일에서 hero copy 위치를 더 아래로 내려 이미지/배경 레이아웃과 맞춘다.
- Change: mobile media query의 `.heroCopy` top 값을 `clamp(7.8rem, 14vh, 12.4rem)`로 조정했다.
- Verification: `npm run lint`, local Next dev server compile.
